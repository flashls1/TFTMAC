# Retired shell source

These Swift files are preserved only as historical donor evidence. They are not
members of the native Xcode target and no build script compiles them. The only
shipping implementation lives under `tftmac/App`, `tftmac/Presentation`, and
`tftmac/Runtime`; `scripts/build-native-app.command` is the sole build authority.
