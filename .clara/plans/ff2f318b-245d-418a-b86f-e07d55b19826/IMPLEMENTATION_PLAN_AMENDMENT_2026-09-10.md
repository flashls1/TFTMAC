# Implementation Plan Amendment — Stop building the lab; produce results

This amendment supersedes the prior execution order where more restrictive. It does not discard prior evidence.

1. Reuse this exact managed change; create no new branch/runtime/app/AVD/SDK.
2. Prove the frozen Sept.10 LKG through the independent `launch-lkg-dev.py` path. Permit one documented zygote namespace clean retry only.
3. Repair OvernightLab OFFICIAL_LIVE identity so the serial is authority-derived `emulator-5586`; synthetic/fault identities cannot enter real execution.
4. Repair startup classification: pre-game launch/ADB/namespace failures are setup/transient/inconclusive classes, never candidate performance HARD_REJECT.
5. Repair resume reconciliation: checkpoint RUNNING + absent supervisor/runtime + restored AVD becomes INTERRUPTED/RECOVERED at a safe boundary.
6. Repair RHI selection proof so explicit Unreal selection/init evidence precedes capability strings; preserve raw classifier output separately.
7. Add only targeted regression tests for items 3–6. No new orchestration feature family.
8. Run one current official CONTROL from LKG through real Tocker gameplay, require frame windows >0, valid runtime identity/telemetry, and verified rollback. Freeze lab feature development when CONTROL_GREEN passes.
9. P1 buffer observability: split invalid-key / conversion-required / buffer-unavailable / allocation / range / format causes; ANGLE component-only build; shortest decisive current-client run.
10. Only if P1 names a dominant safe repairable cause, make one retention repair and require mechanism activation before timing promotion.
11. P2 cache: use existing LKG cache mechanism only; set/readback before TFT PID; matched CONTROL/CACHE/CONTROL comparison; no new backend/capacity architecture.
12. P3 direct Vulkan: full LKG profile + only RHI delta; boot/error screen first; stage 1-1 only if clean; prior missing-attribute/pipeline failure means immediate clean rejection.
13. Funnel winners: static/mechanism -> short Tocker -> extended Tocker -> one full real match. Do not long-test losers.
14. If all three fail, produce current-client causal report and one evidence-backed next candidate only; no random matrix.
15. After CONTROL_GREEN or if validation requires it, remove generated runtime/build artifacts from Git tracking and add precise ignores while preserving local evidence outside source control.
16. Reconcile stale authority documents to distinguish protected Build8 Control from Sept.10 DEV LKG without erasing history.
17. Run production validation, full diff review, LKG identity verification, clean runtime/AVD state verification, final report, checkpoint/publication/delivery, and close only on acceptance evidence.

Hard exclusions: no PBE evidence, no CPU/RAM/compiler sweeps, no MSAA2, no resolution retreat, no presenter optimization, no new app/runtime clone, no broad Vulkan repair loop, no UI redesign, no target redefinition below 60 useful FPS.
