# Wave B v4 — Verified Continuation-Harness Correction

Status: ACTIVE APPEND-ONLY CORRECTION

This correction supplements the original TFTMAC Causal Graphics Optimization plan v1.1 and the Wave B v3 continuation amendment. It does not replace or weaken either authority. The original plan remains SHA-256 `78785b29815dba26a8da1d2ed56e0e9c256d3e7d9cfb4c4857b3c954846e8d2b`; the execution-scope lock remains SHA-256 `dc77b6913d14409adc883dd27a67c42e982c40ab9149a68771b61dc99fae297e`.

## Current authority

- Managed change: `215ec5a3-6554-4ca3-95db-1525433bb20f`.
- Exact clean and remote-aligned head before this correction: `3326792efc694e853312aa7347abcb1edd96575a`.
- Wave B v3 remains preserved byte-for-byte as historical continuation authority.
- The protected rejected Wave B draft remains preserved and is not an implementation source.
- Stock Build 8 remains the default protected control runtime.

## Proven continuation defects

A disposable clone proved that the published Wave B v3 apply/validation package cannot complete from the recorded head without correction. The failures are deterministic and limited to continuation mechanics and generated source:

1. The guarded apply expects a pre-checkpoint head.
2. Four source replacements use non-unique anchors, and one section replacement uses reversed boundaries.
3. The validator searches for a stale Swift expression and counts only one test file while asserting the repository-wide total.
4. The inherited runtime-mode template contains invalid Swift string-interpolation escaping.
5. Generated cleanup, port-list, and lease-release source contains three compile defects.
6. The lease payload nests mode identity while the Wave B contract and test require exact top-level identity fields.

With those corrections applied only in a disposable clone, the unsigned Release build and all 49 native tests passed. No emulator, TFT package, installed app, AVD, runtime binary, or control process was changed or launched.

## Authorized correction

1. Preserve Wave B v3 and protected rejected work unchanged.
2. Place corrected, accepted RuntimeMode, RuntimeModeAuthority, and RuntimeLease templates under this Wave B v4 authority.
3. Correct only the proven apply anchors, generated Swift defects, validator ownership string, and repository-wide test inventory calculation.
4. Guard the apply against exact head `3326792efc694e853312aa7347abcb1edd96575a`, a clean tracked worktree, exact source hashes, and exact accepted external binary identities.
5. Keep `control` as the sole launch-ready default. Keep `advanced_diagnostics` blocked pending a separately receipted controller port. Keep `candidate` blocked.
6. Run the existing production source verifier once after application. PASS requires an unsigned Release build, all 49 native tests, exact packaged registries, Wave B source validation, and zero runtime effects.
7. Update current project facts/checklist only after the verifier proves the changed source.

## Explicit exclusions

- No emulator or TFT launch.
- No diagnostic first boot or controller-port allocation.
- No AVD mutation, runtime installation, application installation, or signing operation.
- No Wave C instrumentation, candidate patch, performance experiment, or final cutover.
- No modification of Riot credentials, the official TFT package, protected history, or accepted external artifacts.

## Exit condition

Wave B v4 is complete when the corrected guarded apply succeeds on the exact recorded head, the production verifier reports the unsigned Release build and all 49 native tests passing, the runtime/process audit proves zero launch effects, and the result is checkpointed locally.
