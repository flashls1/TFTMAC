# Keychain wait blocks saved login and quit — targeted repair

2026-09-06 09:55:32 UTC. Live sample r16-login-stall.sample.txt proves SecItemCopyMatching is waiting in legacy file-keychain decryption on the runtime actor. The earlier polling hypothesis was disproved for this incident and its edits reverted. Chromium's primary source scoped_keychain_user_interaction_allowed.cc documents that SecItem interaction suppression does not work for file keychains (FB16959400); it uses scoped SecKeychainGet/SetUserInteractionAllowed. Existing LAContext suppression alone therefore is insufficient here.

ZoeMC MODE A PLAN_PREFLIGHT: separate credential access, OS authorization, form readiness, actor availability and ordered input. Domains legacy interaction state true/false; scope setup success/failure; read success/denied/missing; app running/stopping; manual generation unchanged/changed. Oracles: no authorization UI caused by automatic reading; unauthorized access fails without secrets; previous process-local interaction setting restored on every return; no credential read blocks the runtime actor; cancellation/manual input cannot type after quit. Holdout: denied read with initial false preserves false, setup failure never reads, delayed success after quit is rejected by existing touch guard, concurrent manual input invalidates existing generation. Proposed scoped suppression and detached read satisfy these oracles; existing LAContext-only implementation fails the first observed case. PLAN_MODEL_PASS (reasoned model; runtime proof pending).

ZenGate V4.1 targeted H1-H6 PASS, Q89 risk5 score84 PASS. Minimum scope: RiotCredentialStore.load scoped legacy no-interaction setting plus off-actor invocation; preserve the existing item/ACL/credential bytes, LAContext, manual fallback, error redaction, and all input guards. No trust-list changes, no keychain unlock, no broad credential framework. Immediate test: invoke production load from a local test executable with an outer deadline, assert previous interaction state restored and output only status; then signed DEV boot and saved sign-in/quit. Success of a helper does not prove app access. A denied app read needs user authorization, never an access-control bypass.

ZEN GATE V4 EXECUTION LOCK — Execute only this approved plan and the minimum changes required to satisfy its stated win condition and acceptance criteria. Every changed file and material diff hunk must trace to the approved requirement, required validation, or proven blocking dependency. Preserve the parent's complete execution lock and final diff audit.
