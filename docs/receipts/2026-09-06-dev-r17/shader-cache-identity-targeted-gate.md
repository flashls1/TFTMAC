# Proven driver launch dependency — 2026-09-06 08:47 UTC

FACT: TFT process 3904 (reuse on) and 3856 (reuse off) terminate in RHIThread while loading the same PrecacheBinaries.scan. Their fresh engine logs report Invalid program binary version followed by RestoreGLProgramFromBinary fatal. Both advertise GL_VERSION OpenGL ES 3.2.0 (ANGLE 2.1.1 git hash: 1166eec4c0b1), as does the working reference. The same GLSL_ES3_1_ANDROID_627821ED64E4C453410284026CCD8D15F25BE4B0 cache namespace is selected. Captures end 08-40-48... and 08-43-03...; raw failures retained. This blocks the immediate actual-TFT producer test.

Source authority: pinned ANGLE Program.cpp deserialize compares GetANGLEShaderProgramVersion() before any binary content; src/program_serialize_data_version.py hashes serialization inputs; Context.cpp initVersionStrings currently exposes only the unchanged Git revision. Smallest repair: append the actual existing shader-program ABI digest to the default GL version string. Keep the negotiated ES major/minor, renderer/vendor, extensions, GL version overrides, quality, settings, driver logic, and all userdata untouched. Do not bypass binary validation or clear caches. Old driver binaries restore their original version string and existing cache namespace.

ZoeMC MODE A targeted: separate shader ABI identity from source Git identity and application cache namespace selection. Domains: same commit/same ABI; same commit/different ABI; different commit/same ABI; ES2/ES3.1/ES3.2; explicit version override. Required behavior: same ABI yields stable suffix; different ABI must differ; actual client version remains truthful; explicit override remains unchanged; old binary rejection remains enabled. Killer input is equal Git revision with unequal shader ABI hashes, observed in the failed experiment. Holdout uses two distinct modeled digest strings sharing a long prefix, varied ES version, and repeated identical input: full digest keeps separation and stability. Dropping the suffix or replacing it with commit hash fails the killer; hardcoding ES3.2 fails the ES2 holdout. Model results are reasoned contract evidence only. PLAN_MODEL_PASS.

ZenGate V4.1 targeted: H1-H6 PASS. Approved launch/driver-cache acceptance dependency, exact owner and source, one isolated source hunk plus reproducible patch/build manifests. Q89, risk5 for Unreal cache-key behavior, score84 PASS. Immediate oracle: rebuild the same driver, launch actual TFT with reuse on, verify changed cache directory, successful shader restore/compile and live process plus its driver events. If directory does not change, this attempt is inconclusive and no additional recorder implementation proceeds. No FPS claim. Parent execution lock applies in full; no broader cache system or game-file mutations authorized by this gate.
