# Targeted execution gate — 2026-09-06

Scope: latest user-approved stable launch, remembered Riot login, recorder admission and frozen first graphics batch. No new renderer architecture is authorized by this gate. No Clara, Control, quality reduction, publication or userdata reset.

FACT: current AVD is the transaction's applied hash; backup and registry agree on the original hash. RuntimeMode validation precedes Runtime service recovery. r7 stopped at Foundation logcat wait; r8 contains bounded kernel-exit handling but never passed AVD discovery. Current code has no Riot credential integration. Actual TFT ANGLE counter export and collector overhead remain unproven.

HISTORY: timestamp-plus-UUID capture names were rejected by a UUID-only driver guard; logcat's exited kernel process left Foundation waiting; manual recovery restored guest driver/profile but omitted host AVD. Regression tests must exercise all three, with the next launch included.

Behavior units / observation / recovery:
1. Launch: immutable authority -> exclusive lease -> absent emulator -> exact-path journal and backup validation -> atomic restore -> readback -> journal removal -> full launch validation. Persist first specific error before cleanup. Unknown authority, hash, owner, write or journal state blocks mutation; retained journal supports retry.
2. Shutdown: stop game -> restore driver/profile -> bounded logcat drain -> stop emulator -> restore AVD -> seal capture. Each actual result is recorded separately; failure cannot become clean shutdown. Retry after interrupted steps must preserve original data.
3. Riot: DEV Keychain load/save -> recognized live form -> mark instance attempted -> validate username focus -> fill -> validate password focus -> fill -> submit once. Authenticated session bypasses; challenge/rejection/unknown fields stop automation. Store state, fill state, submission and authentication are separate. Secret data uses private in-memory input only. Gameplay disables login polling.
4. Recording: current TFT producer identity, actual frame records, native unique delivery, sealed/readable triggered trace, health/loss counters -> diagnostic admission. Missing producer/write failure cannot pass. Counter exposure is not a performance result. Overhead and full lineage remain separate final acceptance gates.
5. Frozen batch: manifest and fixed quality -> real match -> complete drain -> compact report. Correctness failure rejects batch. Gains are exploratory across different matches. No building or expensive analysis during play.

Model domains and transition traces are retained in launch-login-batch-preflight.py. Provenance: authority/path/lease/hash guarantees from current RuntimeMode, RuntimeModeAuthority, RuntimeLease and AVDTransactionGuard; one-attempt/focus and recorder admission are explicit user acceptance; no-launch without validated configuration and no removal before restored readback are integrity oracles. Critical coupling groups: authority/owner/running/journal/backup/current/write; form/focus/secret/attempt/challenge/gameplay; producer/frames/native/identity/seal/readback/health/loss. Each small group is exhausted. No assumptions exclude unknown/conflicting states. Mandatory false-atomicity splits above have distinct outcomes; no known unrepresented decision collision in this bounded model. Shutdown process-vs-Foundation state is retained as an explicit live regression, not claimed covered by the pure model.

ZoeMC MODE A: PLAN_MODEL_PASS, bounded state results in launch-login-batch-preflight.json, including fresh interruption/retry holdout and mutations detecting checksum-before-recovery, running-emulator mutation and unknown-config overwrite. This establishes defined plan behavior, not code correctness or 60 FPS feasibility. Live Riot accessibility labels and actual driver counter activity are empirical test dependencies: inspect the form before typing; test the current process before claiming admission. Stop safely if either is unavailable.

ZenGate V4.1: H1 outcome explicit; H2 exact current source/runtime evidence; H3 unknowns have fail-closed tests; H4 exact DEV authority and reversible config/driver changes; H5 nearest decisive tests; H6 requirement-to-hunk scope. All hard gates PASS. Scores 0..5: alignment5, truth4, ambiguity4, simplicity5, complexity justification4, boundaries5, failure handling4, maintainability4, shipping4, explanation5. Weighted quality88.6; risk penalties4 (remaining live form/counter dependencies); final84.6 PASS. This is a planning judgment, not a measured probability.

Next-action invariant: after exact recovery, immediately launch existing r8 reference. After enduring recovery/error repair, build/test only that set and perform interrupted-relaunch, live startup failure and clean stop/next boot. Then implement and immediately test saved sign-in on the live recognized form. Only then complete missing recorder admission and freeze the existing graphics batch. Three launch/lobby/quit cycles incl cold boot precede user match. Report every30min; stop after60min without runnable decision test. Match/drain exempt from interruption.

ZEN GATE V4 EXECUTION LOCK — Execute only this approved plan and the minimum changes required to satisfy its stated win condition and acceptance criteria. Do not add, refactor, rename, clean up, generalize, optimize, upgrade, reformat, or alter unrelated behavior unless this plan explicitly requires it. Every changed file and material diff hunk must trace to an approved requirement/plan step, required validation, or a proven blocking dependency without which acceptance cannot pass. When an unplanned issue is discovered, perform only the minimum investigation needed to classify it: if it blocks current acceptance, prove the dependency and return the smallest scope expansion through targeted Zen Gate before changing it; if it is a real but non-blocking defect, do not repair it now—save it to the persistent project scratchpad or Zen Gate Discovery Ledger and include it in the final ZEN GATE DISCOVERY REPORT for separate review and Zen Gate repair planning. Once a discovery is shown not to block the current win condition, stop investigating it and return to the approved task. Before completion, audit the final diff and revert every change that cannot be justified by this rule.
