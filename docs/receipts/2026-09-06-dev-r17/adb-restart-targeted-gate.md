# Proven boot dependency — 2026-09-06 09:09 UTC

Observed twice: captures 08-42-11... and 09-06-14... fail before driver mount with adb unable to connect for root: closed. The identical retry previously advanced to TFT. changeHighPerfADBD throws at the transition command before its existing UID verification loop, so an expected transport restart can abort startup without observing its outcome.

Smallest repair: only that exact root/unroot connection-closed error enters the existing UID verification loop. Success still requires the exact requested UID. If the opposite known UID is observed and the original command returned that exact error, resend the same idempotent transition at most once; any other command error remains fatal. Preserve the original specific error on unresolved verification, runtime isolation and ownership, and existing bounded loop. No daemon reconfiguration, broad retry policy, privilege broadening, or unrelated ADB repair.

ZoeMC MODE A: transition request, transport reply, observed privilege are separate behavior units. Domains command succeeds/known closed/other failure; UID requested/opposite/unavailable/unknown; resend unused/used. Oracles: only requested UID succeeds; other failure immediately stops; known-closed plus opposite UID permits exactly one resend; unknown never succeeds. Holdout: closed command actually succeeded (must not resend), then opposite UID stays opposite after resend (must fail); second generic command failure must stop. Ignoring errors broadly or treating command success as UID success fails these cases. PLAN_MODEL_PASS (bounded reasoned model, live branch proof pending).

ZenGate V4.1 targeted H1-H6 PASS; Q89, risk5, score84 PASS. Immediate test: signed DEV boot using unchanged candidate driver; verify root/unroot outcomes, real TFT driver identity, then quit and verify SQLite normalization and rollback. Parent execution lock retained. This replaces the earlier non-blocking classification because the same defect now blocks the immediate required test.
